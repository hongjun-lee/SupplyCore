# 详细设计交叉评审记录（V0.1）

**日期：** 2026-05-02  
**评审范围：** `docs/详细设计/00-11`、`README.md`、`开发实施引用索引-v0.2.md`  
**评审目的：** 检查详设分卷之间的版本引用、实体覆盖、状态字段、跨模块边界和接口/审批/前缀口径是否一致。

---

## 一、评审结论

本轮交叉评审后，`01-11` 详设分卷主体已形成可继续向开发实施交接的结构化基线：

- 版本引用已统一到当前文件版本；未发现仍指向 `05-v0.1`、`06-v0.1/v0.2`、`07-v0.1`、`08-v0.1` 等旧版的有效引用。
- 关键实体覆盖检查通过：C-01~C-11、E-01~E-14、F-01~F-14、R-01~R-11、A-01~A-20 均已在对应分卷中覆盖。
- `interface_push_state` 已统一为财务接口推送状态字段；旧 `nc_push_state` 仅作为历史修订说明出现。
- 07/08/09/10 的 mermaid / code fence 结构检查通过。
- README 详细设计层索引已补齐并更新至当前版本。

---

## 二、本轮已修正问题

| 序号 | 问题 | 处理结果 |
| --- | --- | --- |
| 1 | README 仍指向部分旧版详设：02-v0.3、06-v0.2、07-v0.1、08-v0.1，且缺 09/10/11 条目 | 已更新为 02-v0.6、06-v0.3、07-v0.2、08-v0.2，并补齐 09/10/11 |
| 2 | 10 权限审批流仍按 A-01~A-19 旧口径描述，缺 A-20 approval_instance | 已补齐 A-20 审批实例字段、ERD 挂接、索引建议、业务规则和版本记录 |
| 3 | 05 合同与资金仍使用 `nc_push_state` 字段，与 01/08 的 `interface_push_state` 口径冲突 | 已将 05 升级为 `05-合同与资金详细设计-v0.2.md`，统一替换为 `interface_push_state` |
| 4 | C-09 `summary_state` 与接口推送状态混用，存在“待推送/推送中/已推送”与业务汇总状态混杂 | 已将 C-09 `summary_state` 调整为“待汇总/已汇总/已推付款/已回写实付/已作废”，接口状态另用 `interface_push_state` |
| 5 | 01 中 E-01/E-07/E-13 仍保留较早设备租赁状态口径 | 已按 07-v0.2 回写：E-01 使用 `equipment_status` 完整值域，E-07 补“草稿/已作废”，E-13 补完整 `summary_state` |

---

## 三、自动检查结果

| 检查项 | 结果 |
| --- | --- |
| 00-11 共 12 个详设文件 code fence / mermaid fence 成对检查 | 通过 |
| 05 C-01~C-11 覆盖 | 通过 |
| 07 E-01~E-14 覆盖 | 通过 |
| 08 F-01~F-14 覆盖 | 通过 |
| 09 R-01~R-11 覆盖 | 通过 |
| 10 A-01~A-20 覆盖 | 通过 |
| 旧版引用 grep 检查 | 有效旧引用为 0 |
| `nc_push_state` 硬字段 grep 检查 | 硬字段为 0，仅保留历史修订说明 |

---

## 四、后续建议

1. **下一轮重点做“状态值域总表”收口**  
   建议在 01 或新增附录中集中维护所有业务状态字段，减少后续分卷新增状态时漂移。

2. **对 09/10/11 做业务确认占位扫描**  
   目前 09/10/11 已成型，下一轮可专门检查 `[待业务确认]`、`[待实施配置]`、`[待 NC 落地]` 是否都能追溯到责任部门和回填位置。

3. **形成“详设 V1.0 封版前检查清单”**  
   当前仍是 v0.x 阶段，后续进入 V1.0 前建议按：实体、字段、状态、接口、审批、权限、报表、非功能、引用、占位十类逐项打勾。

---

## 五、一句话结论

本轮交叉评审主要解决了版本索引滞后、A-20 审批实例缺失、05 与 01/08 财务接口字段冲突、C-09 状态分层不清和设备租赁状态回写不一致等问题；修正后，详设 01~11 的主干结构已基本对齐，可继续进入“V1.0 封版前检查清单”和业务确认占位收口。
