- 2026-05-01 将 `docs/概要设计/00-07` 共 8 份 Markdown 概要设计文档批量转换为 Word，输出到 `docs/概要设计/word/`。转换使用项目既有 `scripts/convert_md_to_doc.py`，安装了 `Markdown` 临时 Python 依赖和全局 `mermaid-cli`，并为脚本补充 Puppeteer `--no-sandbox` 配置以适配当前环境下的本地 Mermaid 渲染。结构 QA 结果：8 份 DOCX 均可被 `python-docx` 打开，均包含嵌入图，且无 Mermaid 代码块残留；LibreOffice 因 Homebrew 镜像排队未装成，未完成页面级 PNG 渲染 QA。
- 2026-05-01 起草并收口 `docs/详细设计/开发实施引用索引-v0.2.md`，先明确未来代码仓库应独立于当前文档仓库，当前仓库作为上游设计依据库；文档规定开发参考优先级、详细设计完成前后的直接参考材料、模块级开工条件、代码仓库建议结构、需求变更回写机制和冲突处理规则。开发同事补充了排序语义、多仓/contracts、AI Tool 约束、变更评审和冲突上报机制；已同步文件名与版本号，并调整供应商改文档和 `A-16 ai_call_log` 引用措辞。README 已同步补入详细设计层目录。
