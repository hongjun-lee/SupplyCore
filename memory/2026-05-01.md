- 2026-05-01 将 `docs/概要设计/00-07` 共 8 份 Markdown 概要设计文档批量转换为 Word，输出到 `docs/概要设计/word/`。转换使用项目既有 `scripts/convert_md_to_doc.py`，安装了 `Markdown` 临时 Python 依赖和全局 `mermaid-cli`，并为脚本补充 Puppeteer `--no-sandbox` 配置以适配当前环境下的本地 Mermaid 渲染。结构 QA 结果：8 份 DOCX 均可被 `python-docx` 打开，均包含嵌入图，且无 Mermaid 代码块残留；LibreOffice 因 Homebrew 镜像排队未装成，未完成页面级 PNG 渲染 QA。
- 2026-05-01 起草并收口 `docs/详细设计/开发实施引用索引-v0.2.md`，先明确未来代码仓库应独立于当前文档仓库，当前仓库作为上游设计依据库；文档规定开发参考优先级、详细设计完成前后的直接参考材料、模块级开工条件、代码仓库建议结构、需求变更回写机制和冲突处理规则。开发同事补充了排序语义、多仓/contracts、AI Tool 约束、变更评审和冲突上报机制；已同步文件名与版本号，并调整供应商改文档和 `A-16 ai_call_log` 引用措辞。README 已同步补入详细设计层目录。
- 2026-05-01 按 Spencer 同意的评审意见，将开发同事的 `docs/详细设计/01-数据库逻辑模型-v0.2.md` 收口升级为 `v0.3`：补技术主键与业务单号分离规则、主表/明细表建模原则、核心明细行实体、S-21 库存事务流水、A-20 审批实例、F-14 接口定义表，并同步 ERD、状态值域、索引建议、口径冲突处理、版本日志和 README。实体总数由 119 调整为 135。
- 2026-05-01 V0.3 评审收尾完成，文件重命名为 `01-数据库逻辑模型-v0.4.md`（v0.3 是 untracked 文件，普通 mv 即可，无 git 历史损失），按用户选 A 路径全做评审 6 项：(a) 节 4.10 末尾加字段迁移规则——多物料单据头表禁止保留行级字段、数量金额字段聚合化、单物料业务允许保留但需明确标注，行级字段口径由 02-11 模块详设承接；(b) F-01/F-08/A-15 的 `interface_id` 字段补 `(FK→F-14)` FK 标注；(c) 节 4.5 idempotent_key 中 `interfaceId` 消歧为 `F-14.interface_code` 业务语义键，禁用技术主键；(d) 节 12.7 加 S-21 与 S-13/S-14 同事务原子性硬约束（不允许异步队列、不允许跨事务延迟提交、写入失败整事务回滚，业务单据不进入"已审"状态）；(e) 节 12.8 明确 A-10 以 `instance_id (FK→A-20)` 为权威主键，`business_bill_id + business_bill_type` 仅作冗余索引便捷入口，禁止绕过 A-20 直接挂业务单据；(f) 节十一占位项补 F-14 接口元数据"待联调阶段补齐"行，A-20/S-21 标注为运行态实体不需占位。实体数 135 不变，节十三 V0.4 变更日志已写。同步更新：README 节五的 v0.3 → v0.4 描述补"含字段迁移规则、F-14 FK 闭合、幂等键消歧、库存事务原子性、A-10 挂接位置"；`开发实施引用索引-v0.2.md` 节 6.2 中 v0.3 → v0.4 引用更新。memory/2026-04-24.md 末尾的"[会话间断点]"两条已闭合，处理结果记录在本条。
