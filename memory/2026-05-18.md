- 2026-05-18 重新评审原系统迁移工作：复核 docs/上线迁移方案、02/06 对照清单、02/06 数据采集模板、03 详设、编码规范，以及实现仓库 MaterialImportHandler / InitialStockImportHandler / EF migration。结论：主链路已成立（02 原系统 SQL 输出 legacy_code + original_category_code；M-18 映射到 V1.8 二级分类；MaterialCodeGenerator 生成无横杠新 material_code；06 期初库存按 material_code 字段兼容新编码或旧 legacy_code 反查）。发现并修复一个硬不一致：06 工程师对照清单仍要求 legacy_material_code，而模板和代码使用 material_code 双路径反查；已统一为字段名 material_code、字段值可用旧码。另修正文档中残留 material_code=legacy_code / 8大类/35二类/M-XX 旧口径，代码注释中高敏感物料前缀说明更新为 M-05 IsSafetySpecial + HG 新编码前缀 + M-02- 旧模板兜底。验证：`dotnet test test/Nova.SupplyCores.Application.Tests/Nova.SupplyCores.Application.Tests.csproj --no-build --filter "FullyQualifiedName~DataImport"` 通过 14/14。
- Spencer 明确物料编码存储/展示最终口径：物料编码落库统一无横杠（如 `JD01000001`），展示层再格式化为 `JD-01-000001`；分类编码同理可内部无横杠（如 `JD01`）、展示为 `JD-01`。后续文档和实现按“存储无分隔符、展示加分隔符”统一。
- 2026-05-18 晚间评审下午实施工作：复核实现仓库中午后的提交（审批中心回归修复、库存 5 单据鉴权、VoucherManagement seed、数据质量周/月报、M-18/期初库存迁移页面、perf-baseline、迁移演示等）。验证时发现 `npm run build` 因 `frontend/src/pages/perf-baseline/App.tsx` 中 DevExtreme DataGrid `keyExpr` 使用函数类型失败（TS2322）；已立修为 `keyExpr={["priority", "area"]}`，随后前端 `npm run build` 通过。DataImport 测试继续通过 14/14。当前仍有未提交改动：perf-baseline 1 行立修、InitialStockImportHandler 注释修正、迁移文档口径修正、若干 untracked e2e snapshots/internal prompt 文件需整理。
- 2026-05-18 21:53 Spencer 同意按评审建议修正。已完成收口：实现仓库 `/Users/lihongjun/aizhetech/SupplyCores` 分主题提交 5 个 commit：`1d3daad` 修 perf-baseline DataGrid keyExpr 构建错误；`3031801` 同步 InitialStockImportHandler 高敏感注释；`9274586` 补 3 个 Playwright 视觉 baseline；`b46b017` 补 Sprint 20c raw prompt；`32a1311` 补 second long-term prompt V0.6。文档仓库 `/Users/lihongjun/aizhetech/SupplyCore` 提交 `811eab8` 统一原系统迁移编码口径（存储无分隔符、展示加分隔符；02/06 模板/清单/方案/03详设/编码规范同步），`a91797c` 提交 memory。验证：SupplyCores 前端 `npm run build` 通过；DataImport 应用测试 14/14 通过；两个仓库 `git status` 均 clean。专项检查发现 `migration-m18` 与 `initial-stock-migration` 页面当前明确是 mock 数据，后端 endpoint 仍待接通。
- 2026-05-18 重新评审原系统迁移工作：复核 docs/上线迁移方案、02/06 对照清单、02/06 数据采集模板、03 详设、编码规范，以及实现仓库 MaterialImportHandler / InitialStockImportHandler / EF migration。结论：主链路已成立（02 原系统 SQL 输出 legacy_code + original_category_code；M-18 映射到 V1.8 二级分类；MaterialCodeGenerator 生成无横杠新 material_code；06 期初库存按 material_code 字段兼容新编码或旧 legacy_code 反查）。发现并修复一个硬不一致：06 工程师对照清单仍要求 legacy_material_code，而模板和代码使用 material_code 双路径反查；已统一为字段名 material_code、字段值可用旧码。另修正文档中残留 material_code=legacy_code / 8大类/35二类/M-XX 旧口径，代码注释中高敏感物料前缀说明更新为 M-05 IsSafetySpecial + HG 新编码前缀 + M-02- 旧模板兜底。验证：`dotnet test test/Nova.SupplyCores.Application.Tests/Nova.SupplyCores.Application.Tests.csproj --no-build --filter "FullyQualifiedName~DataImport"` 通过 14/14。
- Spencer 明确物料编码存储/展示最终口径：物料编码落库统一无横杠（如 `JD01000001`），展示层再格式化为 `JD-01-000001`；分类编码同理可内部无横杠（如 `JD01`）、展示为 `JD-01`。后续文档和实现按“存储无分隔符、展示加分隔符”统一。
- 2026-05-18 晚间评审下午实施工作：复核实现仓库中午后的提交（审批中心回归修复、库存 5 单据鉴权、VoucherManagement seed、数据质量周/月报、M-18/期初库存迁移页面、perf-baseline、迁移演示等）。验证时发现 `npm run build` 因 `frontend/src/pages/perf-baseline/App.tsx` 中 DevExtreme DataGrid `keyExpr` 使用函数类型失败（TS2322）；已立修为 `keyExpr={["priority", "area"]}`，随后前端 `npm run build` 通过。DataImport 测试继续通过 14/14。当前仍有未提交改动：perf-baseline 1 行立修、InitialStockImportHandler 注释修正、迁移文档口径修正、若干 untracked e2e snapshots/internal prompt 文件需整理。
- 2026-05-18 21:53 Spencer 同意按评审建议修正。已完成收口：实现仓库 `/Users/lihongjun/aizhetech/SupplyCores` 分主题提交 5 个 commit：`1d3daad` 修 perf-baseline DataGrid keyExpr 构建错误；`3031801` 同步 InitialStockImportHandler 高敏感注释；`9274586` 补 3 个 Playwright 视觉 baseline；`b46b017` 补 Sprint 20c raw prompt；`32a1311` 补 second long-term prompt V0.6。文档仓库 `/Users/lihongjun/aizhetech/SupplyCore` 提交 `811eab8` 统一原系统迁移编码口径（存储无分隔符、展示加分隔符；02/06 模板/清单/方案/03详设/编码规范同步），`a91797c` 提交 memory。验证：SupplyCores 前端 `npm run build` 通过；DataImport 应用测试 14/14 通过；两个仓库 `git status` 均 clean。专项检查发现 `migration-m18` 与 `initial-stock-migration` 页面当前明确是 mock 数据，后端 endpoint 仍待接通。
- 2026-05-18 23:09 pre-compaction flush：Spencer 继续要求把 M-18 / 06 迁移看板落地到真实接口。当前工作上下文指向实现仓库 `/Users/lihongjun/aizhetech/SupplyCores/modules/nova.supplycores`：已新增/修改只读后端分层文件 `Application.Contracts/DataGovernance/IMaterialCategoryLegacyMappingAppService.cs`、`IInitialStockMigrationValidationAppService.cs`，`Application/DataGovernance/MaterialCategoryLegacyMappingAppService.cs`、`InitialStockMigrationValidationAppService.cs`，以及 HttpApi controllers `MaterialCategoryLegacyMappingController.cs`、`InitialStockMigrationValidationController.cs`，暴露固定路径 `GET /api/supply-cores/material-category-legacy-mapping` 和 `GET /api/supply-cores/initial-stock/migration-validation`。权限沿用 `SupplyCoresPermissions.DataGovernance.Default`；A-06 按 `org_id` claim → `Organization.SubGroupId` 做二级集团隔离；M-18 返回 `Mapped/Unmapped/Conflict` summary/items；06 返回三试点单位 + 5 链路矩阵。前端目标文件为 `frontend/src/pages/migration-m18/App.tsx` 与 `frontend/src/pages/initial-stock-migration/App.tsx`，应从 mock 改为真实 fetch，并保留 loading/error/empty。
- 2026-05-18 23:10 pre-compaction flush：本轮已新增测试文件 `test/Nova.SupplyCores.Application.Tests/DataGovernance/MigrationDashboardAppService_Tests.cs`，覆盖 M-18 状态归一/summary 和 06 试点行/链路矩阵聚合。后续恢复时务必先跑验证再对外称“通过”：`dotnet test test/Nova.SupplyCores.Application.Tests/Nova.SupplyCores.Application.Tests.csproj`，以及前端 `cd frontend && npm run build`（必要时再跑相关 Playwright）。当前压缩前只读/写上下文未保留实际 dotnet/npm 输出；如果没有新工具输出，不要继续声称验证已通过。潜在编译关注点：`DateOnly` DTO 序列化、LINQ `GetValueOrDefault`/字典类型、NSubstitute repository queryable mock、E2E 文案仍有“mock/待 backend endpoint”旧断言需同步。
- 2026-05-18 继续推进 SupplyCores 迁移闭环：M-18 映射导入能力已接入 02 物资主数据导入模板，新增 `MaterialCategoryLegacyMapping` sheet，支持 `original_category_code` 唯一校验、`category_code` V1.8 二级分类校验、启用/停用、覆盖更新和冲突台账；`MaterialImportHandler.ApplyAsync` 会先 upsert M-18，再让 Material sheet 通过 `original_category_code` 命中映射生成新物料编码。06 期初库存 `InitialStockImportHandler.ApplyAsync` 已从 `stock_entity_pending_impl` TODO 改为调用 `StockBalanceUpdater.ApplyDeltaAsync`，按 `warehouse.OrgId` 写 S-13 + S-21，幂等键 `initial-stock:{batchId}:{rowNo}:{materialId}:{warehouseId}`；有 M-15 批次实体时带 `BatchId` 同步 S-14，无批次实体时记录 Medium `stock_batch_entity_missing_apply` 但不阻断普通 S-13/S-21。验证：`dotnet build src/Nova.SupplyCores.Application/Nova.SupplyCores.Application.csproj --no-restore -clp:ErrorsOnly` 成功（175 warnings/0 errors）；`dotnet build src/Nova.SupplyCores.HttpApi/Nova.SupplyCores.HttpApi.csproj --no-restore -clp:ErrorsOnly` 成功（4 warnings/0 errors）；`dotnet test test/Nova.SupplyCores.Application.Tests/Nova.SupplyCores.Application.Tests.csproj --no-restore -clp:ErrorsOnly` 通过 1043/0。
